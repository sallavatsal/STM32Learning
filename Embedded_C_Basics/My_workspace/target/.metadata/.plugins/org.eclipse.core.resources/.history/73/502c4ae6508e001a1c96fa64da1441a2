/**
  ******************************************************************************
  * @file    main.c
  * @author  Auto-generated by STM32CubeIDE
  * @version V1.0
  * @brief   Default main function.
  ******************************************************************************
*/

#include<stdio.h>
#include<stdint.h>
#include "main.h"

int main(void)
{
	RCC_AHB1ENR_t volatile *const pClkCtrlreg = (RCC_AHB1ENR_t*)(0x40023830);		// Clock enable
	GPIOx_MODE_t volatile *const pPortDModereg = (GPIOx_MODE_t*)(0x40020C00);		// GPIOD Control Mode
	GPIOx_ODR_t volatile *const pPortDOutreg = (GPIOx_ODR_t*)(0x40020C14);			// GPIO D Output Data register
	GPIOx_ODR_t volatile *const pPortDOutreg = (GPIOx_IDR_t*)(0x40020C00);			// GPIO D Input Data register
	GPIOx_ODR_t volatile *const pPortDOutreg = (GPIOx_PUPDR_t*)(0x40020C00);		// GPIO D Input Data register

	pClkCtrlreg->GPIOD_EN = 1;														// GPIOD enabled
	GPIOx_MODE_t->PIN_0 = 1;														//
	GPIOx_MODE_t->PIN_1 = 1;														//
	GPIOx_MODE_t->PIN_2 = 1;														//
	GPIOx_MODE_t->PIN_3 = 1;														//
	GPIOx_MODE_t->PIN_8 = 0;														//
	GPIOx_MODE_t->PIN_9 = 0;														//
	GPIOx_MODE_t->PIN_10 = 0;														//
	GPIOx_MODE_t->PIN_11 = 0;														//

	while(1){

	}

	return 0;
}
